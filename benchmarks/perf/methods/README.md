# Method Suites

This folder contains estimator-family performance suites, one directory per
method (for example `npreg`, `npcdens`, `npudens`).

Each method directory contains parameterized benchmark scripts, combo runners,
and report/comparison helpers.
