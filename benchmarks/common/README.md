# Common Benchmark Utilities

Place reusable benchmark helpers here (data generation, summary formatters,
shared argument parsing).

MPI-specific broadcast/setup helpers should stay in `npRmpi` only.
