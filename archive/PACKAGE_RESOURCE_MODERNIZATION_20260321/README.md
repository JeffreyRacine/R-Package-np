This archive records the `npRmpi` Phase 1 package-resource modernization
tranche run on 2026-03-21.

The legacy package-shipped vignette sources were removed from the live
`vignettes/` surface so the package can ship a lean installed operational
getting-started vignette while the gallery remains the long-form teaching
surface for MPI/platform details.

Archived legacy vignette sources:

- `npRmpi.Rnw`
- `npRmpi.bib`
- `np_faq.Rnw`
- `np_faq.bib`

The follow-on startup-banner and front-door `.Rd` cleanup tranches should
update the remaining references to retired vignette names after the new
installed vignette surface is in place and validated.
