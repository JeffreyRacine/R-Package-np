This archive records the `np` Phase 1 package-resource modernization tranche
run on 2026-03-21.

The legacy package-shipped vignette sources were removed from the live
`vignettes/` surface so the package can ship a lean installed getting-started
surface while the gallery remains the long-form teaching surface.

Archived legacy vignette sources:

- `np.Rnw`
- `np.bib`
- `np_faq.Rnw`
- `np_faq.bib`
- `entropy_np.Rnw`
- `entropy_np.bib`

The follow-on startup-banner and front-door `.Rd` cleanup tranches should update
the remaining references to retired vignette names after the new installed
vignette surface is in place and validated.
